CREATE TABLE users(id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY, username VARCHAR(32) NOT NULL UNIQUE, salt VARCHAR(64) NOT NULL, passwordhash VARCHAR(64) NOT NULL, INDEX (username));
CREATE TABLE history(id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY, user_id INT UNSIGNED NOT NULL, query VARCHAR(2048) NOT NULL, INDEX (user_id));